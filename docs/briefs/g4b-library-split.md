# G4b — replay logic as a library crate; shared fixtures export; contract hook through `play`

## 1. Read first
`AGENTS.md`; on `main` after G4 (#8): `crates/slingfall_replay/` (`src/main.cairo` holds `play`, `step_shot`,
`decode`, `outputs`; `Scarb.toml` with `[lib]` + four executables, nested `[workspace]`),
`crates/slingfall_contract/src/simulate.cairo` + `simulate/replay_hook.cairo` (the duplicated loop),
`crates/slingfall_level/src/level/fixtures.cairo` (`#[cfg(test)]`), the copies in
`crates/slingfall_replay/src/main/fixtures.cairo` and `crates/slingfall_contract/src/submit/fixtures.cairo`,
`tools/levelc/levelc.py to-cairo`. G4's and G7's escalations 2-3 are this lot.

## 2. Scope (allowlist)
New crate `crates/slingfall_game/**` (name final), root `Scarb.toml` (add the member and the workspace
dependency), `crates/slingfall_replay/**` (thin: executables + `TraceObserver` only), `crates/slingfall_contract/**`
(manifest dependency + hook), `crates/slingfall_level/**` (fixtures export), `steps/**` of the touched crates,
`.github/workflows/ci.yml` ONLY to add the new crate's test job to the matrix and `all-checks`.

## 3. Work
1. `slingfall_game` (library, snforge-tested, in the root workspace): `play`, `Observer`, `NoopObserver`,
   `step_shot`, `ShotProgress`, `decode`, `outputs`, `level_over`, and the chunk logic (`ChunkState`, the
   `init` / `step_chunk` bodies) moved out of `slingfall_replay`; `slingfall_replay` keeps only the
   four `#[executable]` entry points and `TraceObserver` (or move the observer too if the nested package can
   depend on the game crate by path: it can, like it depends on the rules). Bit-exact: the G4 goldens
   (`main` outputs, chunk chains, contract `simulate` golden) must not change.
2. Contract: `ActiveHook` = an impl calling `slingfall_game::play` with `NoopObserver`; delete the duplicated loop.
3. Fixtures: a non-test `pub mod fixtures` in `slingfall_level` (generated by `levelc.py to-cairo` into
   `src/level/fixtures.cairo`, no longer `#[cfg(test)]`, with a `--check` in `tools/levelc`), used by the game,
   replay and contract tests; delete the two copies.
4. Steps: `steps.py` snapshots of the new crate (move the replay's `steps_*` probes that test the logic).

## 4. Budget
Zero change of `main`'s steps (measure `scarb execute` on pile10 reference before / after; ≤ 0.1 %).

## 5. Tests
All existing tests pass unchanged (moved, not rewritten); `snforge test -p slingfall_game`; the contract golden.

## 6. Definition of done
`AGENTS.md` §6 on every touched crate (crate-scoped; the replay package with `--manifest-path`); conventional
commits with the trailer `Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>`; push `feat/g4b-library-split`;
`gh pr create`; `gh pr checks --watch` until green; never merge; `REPORT.md` (Summary · Layout · Steps before / after ·
Deviations · Escalations · PR URL).

## 7. Work autonomously, do not ask questions, do not widen the scope.
